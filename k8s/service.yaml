apiVersion: v1
kind: Service
metadata:
  name: http-server
  namespace: http-server-devops
  labels:
   app: http-server
spec:
  type: NodePort
  selector:
    app: http-server
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
